📄 Fullstack CRUD App
Overview:
Fullstack CRUD app using Node.js + Express + MongoDB (backend)
HTML + JavaScript frontend for user interaction
Setup:
Install dependencies: npm install express mongoose cors
Run backend server: node server.js
Open index.html in browser for frontend
API Endpoints:
POST /users → Add a user
GET /users → Get all users
PUT /users/:id → Update a user
DELETE /users/:id → Delete a user
Usage:
Test APIs via Postman or the frontend
Add users using input fields and “Add User” button
Update/Delete users using respective buttons in the list
